<h1>Notifications</h1>
<% if @notifications.size == 0 %>
  You have no new notifications
<% else %>
  <% @notifications.each do |notification| %>
      <%= render "notifications/#{notification.notifiable_type.underscore}/#{notification.action.underscore}", actor: notification.actor, notifiable: notification.notifiable  %>
      <hr/>
  <% end %>
<% end %>